<Window x:Class="PaintUseCanvas.Pages.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MessageDialog" Height="150" Width="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TxtMessage" Text="New message" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center" 
                   TextWrapping="Wrap" Width="230" TextAlignment="Center" Margin="5"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="BtnOk" Grid.Column="0" Content="Ok" Margin="20,10,20,10" Click="BtnOk_Click"/>
            <Button x:Name="BtnCancel" Grid.Column="1" Content="Cancel" Margin="20,10,20,10" Click="BtnCancel_Click"/>
        </Grid>
    </Grid>
</Window>
